RewriteEngine On

# Force HTTPS — redirects all HTTP traffic to HTTPS
RewriteCond %{HTTPS} off
RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [R=301,L]

# Disable directory listing — prevents file browsing in any subdirectory
Options -Indexes
